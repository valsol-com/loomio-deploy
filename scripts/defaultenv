# this is the hostname of your app
CANONICAL_HOST=REPLACE_WITH_HOSTNAME

# the number of dots in your hostname
TLD_LENGTH=REPLACE_WITH_TLD_LENGTH

# smtp settings
SUPPORT_EMAIL=support@REPLACE_WITH_HOSTNAME
SMTP_DOMAIN=REPLACE_WITH_HOSTNAME
SMTP_SERVER=smtp.something.com
SMTP_PORT=465
SMTP_USERNAME=smtpusername
SMTP_PASSWORD=smtppassword

FAYE_URL=https://REPLACE_WITH_HOSTNAME:4443/faye
REPLY_HOSTNAME=REPLACE_WITH_HOSTNAME

# ssl certificates
LOOMIO_SSL_KEY=/certificates/live/REPLACE_WITH_HOSTNAME/privkey.pem
LOOMIO_SSL_CERT=/certificates/live/REPLACE_WITH_HOSTNAME/fullchain.pem

# helper bot is the account which welcomes people to their groups.
HELPER_BOT_EMAIL=noreply@REPLACE_WITH_HOSTNAME
